# 『カミサマ、いま起きてる？』プロトタイプ設計書（仮タイトル）

> このファイルは Claude Code 向けの実装仕様です。プロトタイプの目的は
> **「現実時間で勝手に進む町」と「ちょっかいで運命が変わる」手触りの検証** のみ。
> 見た目・演出・課金・ガチャは作らない。
>
> ※ プロト後に練り直したコンセプトは [CONCEPT.md](CONCEPT.md)（コンセプト v2）。
> v2 の①②（ちょっかい4種類・教義・聖典など）はゲームに反映済みで、この設計書の 7章（ちょっかい）や祈りまわりより CONCEPT.md が新しい。

---

## 1. コンセプト（1行）

スマホの中に湧いた小さな町。住民は画面の外にいる「カミサマ（プレイヤー）」を知っていて、祈ったり呪ったりしている。プレイヤーの気まぐれな干渉が、住民にとっては運命そのもの。

**トーン**：ブラック・シュール。住民は淡々としていて理屈っぽい。善意が悲劇になり、意地悪が救いになることもある。

---

## 2. 技術仕様

| 項目 | 内容 |
|---|---|
| エンジン | Godot 4.x（最新安定版） |
| 言語 | GDScript |
| 対象 | まずPCで動作確認 → Android書き出し |
| 画面 | 縦持ち。基準解像度 **360×640** |
| ストレッチ | `display/window/stretch/mode = viewport`、`scale_mode = integer` |
| テクスチャ | デフォルトフィルタ `Nearest`（ピクセルがぼやけないように） |
| キャラサイズ | 16×16px 基準 |
| フォント | 日本語ピクセルフォント（DotGothic16 など、OFLライセンスのもの） |
| 素材 | **プロトタイプは仮素材**（単色の四角 or 簡単なドット）。後で差し替え前提 |

---

## 3. フォルダ構成

```
res://
├─ project.godot
├─ scenes/
│   ├─ Main.tscn          # ルート。町＋UIを配置
│   ├─ Town.tscn          # 町の背景・建物・住民の配置
│   ├─ Resident.tscn      # 住民1人分（スプライト＋吹き出し）
│   └─ ui/
│       ├─ HUD.tscn       # 上部：時刻・信仰度 / 下部：ちょっかいボタン
│       ├─ LogPanel.tscn  # 住民ログの一覧
│       └─ AwayReport.tscn# 「留守中の出来事」ポップアップ
├─ scripts/
│   ├─ autoload/
│   │   ├─ GameClock.gd   # 現実時刻の取得・デバッグ用時間加速
│   │   ├─ Sim.gd         # 町の状態とシミュレーション本体
│   │   ├─ LogManager.gd  # ログ生成・保持
│   │   └─ SaveManager.gd # セーブ／ロード
│   ├─ resident.gd
│   └─ ui/*.gd
├─ data/
│   ├─ residents.json     # 初期住民データ
│   └─ log_templates.json # ログ文テンプレート
└─ assets/
    ├─ sprites/
    └─ fonts/
```

**重要**：シミュレーション（`Sim.gd`）と表示（シーン）を分離する。オフライン進行で表示なしにシミュレーションだけ回すため。

---

## 4. 時間システム

- **ゲーム内時刻 = 現実の端末時刻**（`Time.get_datetime_dict_from_system()`）
- シミュレーションの最小単位：**1ティック = 現実10分**
- 起動中は10分ごとにティックを進める（リアルタイム）
- 住民の行動は「現実の何時か」で決まる

| 時間帯 | 住民の基本行動 |
|---|---|
| 0:00–6:00 | 睡眠 |
| 6:00–9:00 | 起床・朝の祈り |
| 9:00–17:00 | 仕事 |
| 17:00–21:00 | 自由行動・集会 |
| 21:00–24:00 | 帰宅・就寝 |

### 4.1 オフライン進行
1. アプリ終了・バックグラウンド移行時に `last_seen`（UNIX時刻）を保存
   - `NOTIFICATION_APPLICATION_PAUSED` と `NOTIFICATION_WM_CLOSE_REQUEST` の両方で保存
2. 起動・復帰時に `now - last_seen` を計算
3. 経過時間ぶん、10分ティックを**表示なしで一括実行**
4. 上限は **72時間**（それ以上は72時間として扱う）
5. 実行中に起きた出来事を集め、`AwayReport` で「留守中の出来事」として表示

### 4.2 カミサマ不在のルール
- プレイヤーがアプリを開いていない時間 = **カミサマが寝ている時間**
- 不在が長いほど町全体の `god_presence`（後述）が下がる
- アプリを開いた瞬間、住民全員が空を見上げる演出（プロトでは吹き出し「!」でOK）

### 4.3 デバッグ機能（必須）
- 時間加速ボタン：×1 / ×60 / ×600
- 時刻オフセット：任意の時刻に飛ぶ
- 「N時間放置したことにする」ボタン（オフライン進行のテスト用）
- デバッグUIはビルドフラグで非表示にできるようにする

---

## 5. 町の状態（Sim.gd）

```
town:
  day: int                 # 町ができてから何日目か
  god_presence: 0..100     # カミサマの存在感。ログイン頻度・干渉で上下
  weather: "sunny" | "rain"
  residents: [Resident]
  graves: [Grave]
  last_seen: int (unix)
```

### 5.1 住民データ

```
Resident:
  id: string
  name: string
  role: string             # パン屋、預言者見習い 等
  faith: -100..100         # カミサマへの感情。+は崇拝、-は憎悪
  health: 0..100
  mood: 0..100
  state: "sleeping" | "working" | "wandering" | "praying" | "dead"
  traits: [string]         # 性格タグ（ログ生成・イベント判定に使う）
  pos: Vector2i            # 町内のタイル座標
```

### 5.2 墓
住民が `health <= 0` で死亡 → 墓になる。
```
Grave:
  resident_id, name, day_of_death,
  cause: string,           # "落雷" "病" "カミサマにつつかれた" 等
  epitaph: string          # 遺言1行（テンプレから生成）
```
墓は町に残り、タップで遺言を読める。

---

## 6. 初期住民（5人）

| 名前 | 役割 | 初期faith | traits | 一言 |
|---|---|---|---|---|
| トメ | パン屋 | +40 | 信心深い, 働き者 | 毎朝パンを1個、空に供える |
| カズ | 預言者見習い | +80 | 狂信的, 思い込みが強い | カミサマの行動を全部「お告げ」と解釈する |
| ノブ | 学者 | -20 | 懐疑派, 理屈っぽい | カミサマは自然現象だと主張している |
| セン | 墓守 | 0 | 無関心, 淡々 | 墓が増えると少し嬉しそう |
| ミミ | 子ども | +10 | 好奇心旺盛, 無邪気 | 空に向かって手を振る |

新しい住民の誕生・転入は**プロトでは実装しない**（5人で検証）。

---

## 7. ちょっかい（プロトで実装するのは2つ）

### 7.1 つつく（住民をタップ）
結果はランダム。**住民にとっては善意か悪意か区別がつかない**のがポイント。

| 結果 | 確率 | 効果 |
|---|---|---|
| 癒やし | 35% | health +30, faith +10 |
| 驚かす | 30% | mood -20, faith ±10（traitで変動） |
| 啓示 | 20% | 住民が「お告げ」を受けたと思い込む。faith +20、周囲にも伝播 |
| 災い | 15% | health -40。0以下なら死亡 |

- 同じ住民を短時間に連打すると災い確率が上がる
- `god_presence` +2

### 7.2 雨を降らせる（ボタン）
- 3時間（18ティック）雨になる
- 状況で意味が変わる：
  - 平常時 → 全員 mood -5
  - 集会中（17–21時）→ 集会中止、faith -5
  - トメ（パン屋）→ 売上減、mood -10
  - ノブ（学者）→ 「ほら、ただの気象だ」とfaith変化なし
- クールダウン：現実1時間

### 7.3 何もしない（暗黙の干渉）
- 祈っている住民に一定時間応えないと faith が下がる
- `god_presence` が 20 未満になると、住民の誰かが「カミサマは死んだ」と言い出す（ログイベント）

---

## 8. 自動イベント（放置中に起きること）

各ティックで低確率で判定。プロトでは以下の6種類のみ。

| イベント | 条件 | 内容 |
|---|---|---|
| 朝の祈り | 6–9時 | faith > 30 の住民が祈る |
| 病気 | ランダム（低確率） | health -20 |
| 口論 | faith の差が大きい2人が同じ場所 | 両者 mood -10 |
| 布教 | カズが起きている | 近くの住民の faith を +5 |
| 反論 | ノブが起きている | 近くの住民の faith を -5 |
| 神は死んだ説 | god_presence < 20 | 誰かが宣言。全員 faith -10 |

---

## 9. ログ

- 画面下部に最新3件を表示、タップで全件（最大200件）
- **住民目線・淡々とした文体**
- `log_templates.json` にテンプレートを持ち、`{name}` `{target}` 等を差し込む

例：
```
「トメが今朝もパンを空に供えた。カミサマは受け取らなかった。」
「カズいわく、昨日の雷はノブへの警告らしい。」
「ノブが倒れた。カミサマにつつかれた直後だった。本人は偶然だと言い張った。」
「センが新しい墓を掘った。心なしか手際がいい。」
「ミミが空に手を振った。今日は誰も振り返さなかった。」
「カミサマが3日ぶりに起きた。町は少しざわついた。」
```

### 9.1 留守中の出来事（AwayReport）
- 復帰時にポップアップ表示
- 出来事が多い場合は重要度の高いもの（死亡 > 神は死んだ説 > 口論 > その他）から最大5件＋「ほか N 件」
- 冒頭文は不在時間で変える
  - 1時間未満：なし（ポップアップ出さない）
  - 1–12時間：「カミサマが眠っている間に」
  - 12–48時間：「カミサマが長く眠っている間に」
  - 48時間以上：「カミサマがいない間に」

---

## 10. 画面構成（360×640）

```
┌──────────────────────┐
│ 9/23 19:56  ☀  存在感 ▮▮▮▯ │ ← HUD上部
├──────────────────────┤
│                              │
│      町（俯瞰ドット絵）        │ ← 住民が歩き回る
│      住民・建物・墓            │    タップでつつく
│                              │
├──────────────────────┤
│ ・トメがパンを供えた           │ ← 最新ログ3件
│ ・カズがお告げを受けた         │
│ ・雨が降り始めた               │
├──────────────────────┤
│  [雨を降らせる]   [ログ]       │ ← 操作ボタン
└──────────────────────┘
```

住民をタップすると簡易ステータス（名前・役割・faith・health）が出て、その上で「つつく」を選ぶ（誤タップ防止）。

---

## 11. セーブ

- `user://save.json` に町の状態全体とログを保存
- 保存タイミング：ティックごと、ちょっかい実行時、アプリ終了・バックグラウンド移行時
- バージョン番号をセーブに含める（後の形式変更に備える）

---

## 12. 実装マイルストーン

1. **M1 町の表示**：360×640の画面、仮背景、住民5人がランダムに歩く
2. **M2 現実時間**：時刻で住民の行動が変わる。HUDに時刻表示。デバッグ用時間加速
3. **M3 ちょっかい**：つつく・雨。ステータス表示とログ出力
4. **M4 セーブとオフライン進行**：放置→復帰で留守中の出来事が表示される
5. **M5 信仰の揺らぎ**：自動イベント6種、god_presence、神は死んだ説、死亡と墓
6. **M6 Android書き出し**：実機で動作確認

各マイルストーンごとに動く状態でコミットする。

---

## 13. プロトでは作らないもの

- ガチャ・課金・キャラカード図鑑（プロトに限らず、方針として入れない。CONCEPT.md 9章）
- 住民の誕生・転入
- 聖典（プレイヤー行動の記録書）
- 夢を見せる・振る・息を吹くなどの追加ちょっかい
- プッシュ通知
- サーバー・時刻改ざん対策
- 本番アート・サウンド

---

## 14. プロトで確かめたいこと

- 放置→復帰したとき「何が起きたんだろう」とワクワクするか
- つつく結果がランダムでも、干渉したくなるか（理不尽さが面白さになっているか）
- 淡々としたログだけで住民に愛着がわくか
- 1日に何回くらい開きたくなるか
